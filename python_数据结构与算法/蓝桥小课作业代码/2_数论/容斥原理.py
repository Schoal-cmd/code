'''
代码实现思路 对于简单的情况，我们可以直接使用公式计算。对于较复杂的情况，通常使用二进制枚举来实现：
使用二进制表示每个集合是否被选中
根据选中集合的个数确定符号（奇数个为正，偶数个为负）

二进制枚举：
在二进制枚举中，每个候选解可以用一个二进制数来表示，其中每一位对应于一个问题中的元素。对于一个有n个元素的集合，我们可以使用从0到2^n-1的整数进行二进制枚举，每个整数的二进制表示中的每一位对应集合中的一个元素，1表示该元素被选中，0表示该元素未被选中。
例如，假设我们有一个包含5个元素的集合，我们可以使用0到31（即2^5-1）之间的整数进行二进制枚举。数字3的二进制表示为“0011”，这意味着第0个和第1个元素被选中（假设从0开始计数）。同样地，数字13的二进制表示为“1101”，这意味着第0个、第2个和第3个元素被选中。
二进制枚举的优点在于它可以高效地遍历所有可能的解，而不需要显式地生成每个解。这使得它在处理大量数据时非常有用，因为它可以显著减少所需的内存和计算时间。
这种技术通常用于解决组合问题，如排列、组合、子集问题等，其中需要检查所有可能的候选解以找到满足特定条件的解。
'''

for i in range(1<<3):
    print(i)